<h1>Log New Mission</h1>


<% if flash[:error] %>
<%flash[:error].each do |e|%>
<%= e %>
<% end %>
<% end %>

<%= form_for @mission do |f|%>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :scientist_id%>
<%= f.collection_select :scientist_id, Scientist.all, :id, :name %>
<%= f.label :planet_id%>
<%= f.collection_select :planet_id, Planet.all, :id, :name %>

<%= f.submit %>
<%end%>